<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymonics">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddRewardActivity"></activity>
        <activity android:name=".AddMisiActivity" />
        <activity android:name=".AddCleaningServiceActivity" />
        <activity android:name=".KelolaRewardActivity" />
        <activity android:name=".KelolaMisiActivity" />
        <activity android:name=".KelolaCleaningServiceActivity" />
        <activity android:name=".EasterEggActivity" />
        <activity android:name=".DetailRewardActivity" />
        <activity android:name=".DetailLaporanActivity" />

        <service android:name=".BroadcastService" />

        <activity android:name=".LaporanActivity" />
        <activity android:name=".RewardActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".DetailMissionActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".CleaningServiceActivity" />
        <activity android:name=".MissionActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>